node_modules/
coverage/
.env
.env.local
.DS_Store
*.log
src/data/*.db*

# This project uses yarn (yarn.lock is the committed lockfile) -- an
# npm-generated lockfile here is a stray artifact, not project state.
package-lock.json
